# GUI框架
PySide6==6.10.1
shiboken6==6.10.1

# 其他依赖（如果需要的话）
# 标准库依赖已包含在Python中，无需额外安装
# - os, sys, shutil, hashlib, threading, json, datetime, time, queue, typing

# 开发依赖（可选）
# pytest>=7.0.0
# black>=23.0.0
# flake8>=6.0.0
